[package]
name = "ags"
version = "0.7.0"
edition = "2024"

[dependencies]
base64 = "0.22"
dirs = "6"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
url = "2"

[dev-dependencies]
tempfile = "3"
